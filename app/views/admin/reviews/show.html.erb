<div>
  <%= image_tag @review.image,size: "300x500" %>
  <p>商品名：<%= @review.item_name %></p>
  <p>説明：<%= @review.review %></p>
  <p>ユーザーネーム：<%= @review.customer.name %></p>
  <p>投稿日：<%= @review.created_at.strftime('%Y/%m/%d') %></p>
  <!--<-% if @post_image.favorited_by?(current_user) %>-->
  <!--  <p>-->
  <!--    <-%= link_to post_image_favorites_path(@post_image), method: :delete do %>-->
  <!--     ♥<-%= @post_image.favorites.count %> いいね-->
  <!--    <-% end %>-->
  <!--  </p>-->
  <!--  <-% else %>-->
  <!--  <p>-->
  <!--    <-%= link_to post_image_favorites_path(@post_image), method: :post do %>-->
  <!--     ♡<-%= @post_image.favorites.count %> いいね-->
  <!--    <-% end %>-->
  <!--  </p>-->
  <!--<-% end %>-->
</div>

<div>
  <p>コメント件数：<%= @review.comments.count %></p>
  <% @review.comments.each do |comment| %>
    <%= comment.customer.name %>
    <%= comment.created_at.strftime('%Y/%m/%d') %><%= comment.comment %>
      <%= link_to "削除", admin_review_comment_path(comment.review, comment), method: :delete, data: {confirm: "削除しますか？"} %>
  <% end %>
</div>

   <%= link_to '戻る', admin_reviews_path, class: "btn btn-success px-3", style:"font-weight:bold;" %>
   <%= link_to "削除", review_path(@review), method: :delete, class: "btn btn-danger px-3" %>